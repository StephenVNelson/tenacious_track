document.addEventListener("turbolinks:load", function() {
  $('#exercise-lookup').autocomplete({
    source: "exercise/autocomplete",
    minLength: 4,
    select: function (event, ui) {
      $('#exercise-lookup').val(ui.item.value);
    }
  });
});
